import { IconButton } from "@material-ui/core";
import AttachMoneyIcon from "@material-ui/icons/AttachMoney";

const Tips = () => {
  return (
    <IconButton disabled>
      <AttachMoneyIcon />
    </IconButton>
  );
};

export default Tips;
